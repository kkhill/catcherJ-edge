
- name: rule1
  event: state_updated
  conditions:
    states:
      - thing: lovely
#        on: on
        from: "off"
        to: "on"
    properties:
      - thing: lovely
        property: brightness
        operation: ">"  # > >= < <= in
        value: 50

  actions: # service name
    services:
      - name: open
        thing: lovely