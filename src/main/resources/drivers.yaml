
Demo: # driver name
  pkg: demo # pkg in com.kkhill.driver
  entry: DemoDriver # class name which extends plugin / addon / driver
  config:
    ip: 127.0.0.1
    port: 8080
    name: hello

Demo1:
  pkg: demo1
  entry: Demo1Driver
  config:
    ip: 127.0.0.1
    port: 8080
    name: word

DemoLight:
  pkg: demolight
  entry: DemoLightDriver
  config:
    ip: 127.0.0.1
    port: 8081
    name: word
